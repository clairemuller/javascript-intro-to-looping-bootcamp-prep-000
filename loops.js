 const forLoop = (array) => {
   for (let i = 0; i < 25; i++) {
    return array;
     if (i === 1) {
       return "I am 1 strange loop.";
     } else {
       return "I am ${i} strange loops.";
     }
   }
 };